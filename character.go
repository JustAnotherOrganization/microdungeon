package microdungeon

/*
func (c *Character) Wield(weapon *Weapon) error {
    // TODO: weapon restrictions.

    if weapon.IsWielded() {
        return errors.Errorf("%s is already wielded", weapon.Name)
    }

    if wielded := c.Wielding); wielded != "" {
        return errors.Errorf("%s is already wielded", wielded)
    }

    c.Wielding = weapon.Name
    weapon.IsWielded = true
}
*/
